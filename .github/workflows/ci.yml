name: CI

on:
  push:
    branches:
      - '**'

  pull_request:

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Java
        uses: actions/setup-java@v1

        with:
          java-version: 1.14

      - name: Build site
        run: sh build.sh
